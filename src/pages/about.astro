---
import { redirect } from 'astro:middleware';

export function getStaticPaths() {
  return [{ params: {}, props: {} }];
}

export async function get({ redirect }) {
  return redirect('/');
}
---

<!-- This page redirects to the main portfolio page -->